# 7100954 基地原始位置稳定回退

## 现场根因

- 7100953 实车按 `6 -> 1 -> C -> 2 -> 7 -> P -> Q -> S -> A -> V -> Y` 后停在 `BASE_POS_ONLY_ALIGN`。
- 无线日志中全局视觉位置包持续推进，坐标稳定在约 `(0.5,5.5)`，但边界处 `pose_valid` 在 0/1 之间闪烁。
- 原基地门控同时要求 `vision.pose_valid` 和 `point.vision_position_stable`，因此连续有效帧永远无法满足；电机保持锁定、PWM=0。这不是速度120、电机方向或编码器故障。

## 7100954 修改

- 仅基地位置对齐阶段新增原始位置稳定器，不改变赛道运行、推箱、编码器方向和普通视觉门控。
- 条件：全局链路在线、位置包持续推进、数据年龄不超过500ms、坐标仍位于所选基地锚点半径1.2格内。
- 连续3个新位置包的相邻坐标变化均不超过0.2格后，即使 `pose_valid` 闪烁也允许进入原有基地位置对齐。
- 视觉角度仍完全屏蔽；基地航向继续固定为地图90度并由IMU保持。
- 每次基地校准运动完成后重置稳定器，必须重新取得3个新位置包才允许复查。

## 构建与回退

- MCU识别号：`7100954 / BASE_RAW_POS_STABLE_FALLBACK`。
- 正式构建：0 Error，0 Warning。
- AXF：`project/mdk/Objects/rt1064.axf`。
- SHA256：`3AE333FF97D6B493D8C9BB92C3EB7FB154D731FF37BEA334B02AC7F71A9E1D36`。
- 修改前备份：`project/_codex_backups/checkpoint_7100953_before_base_raw_pos_fallback_20260722_020527`。

## 下一次实车流程

重新下载并完全断电上电后确认启动行为号7100954，然后执行：

`6 -> 1 -> C -> 2 -> 7 -> P -> Q -> S -> A -> V -> Y`

期望：`BASE_POS_ONLY_ALIGN` 最多等待约3个位置包后开始校准；随后进入 `BASE_ALIGN_RUNNING` 或直接进入 `EXIT_PREP`，不得继续长期保持 `point=LOCKED`、`pwm=(0,0,0)`。

