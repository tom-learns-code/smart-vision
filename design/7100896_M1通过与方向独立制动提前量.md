# 7100896 M1通过与方向独立制动提前量

更新时间：2026-07-19

## 当前版本

- MCU识别号：`7100896`
- 当前启动阶段：`DIRECTIONAL_BRAKE_M2`
- 当前编译模式：`CONTROL_DEBUG_ENABLE=3U`
- 视觉端未修改：`oa_id=7100422`
- 修改前备份：`project/_codex_backups/checkpoint_7100895_m1_complete_before_direction_brake_20260719_004709`
- 最终固件：`project/mdk/Objects/rt1064.axf`
- AXF SHA256：`C755B5B2044ACCB68F5EAF07DBE0D24487A8868B733067DEDAC17C7F5980A980`
- 模式0/1/2/3：ARMClang 6.16全部`0 Error(s), 0 Warning(s)`

## M1结论

7100895使用300ms起步补偿完成三次相互独立的左移一格测试，三次均自主起步、无需手推、未触发STALL；`assist_mask`三次均为`0x07`。编码器纵向分别为164.8、178.9、174.7mm，平均172.8mm；距离偏短是统一50mm制动提前量问题，不是起步失败。

第3次数据：编码器174.7/4.1mm，视觉180.0/0.0mm，视觉相对编码器5.3/-4.1mm；IMU相对航向1.9度，最大航向1.9度；稳定视觉6帧、bad=0；`assist_samples=14`、`stall=0ms/5.8mm`、`txdrop=0`。M1以3/3正式通过，禁止继续重复单独左移起步测试。

视觉第3次起点角85.2度、终点94.8度，而IMU变化仅1.9度，再次证明视觉角度只适合停车后辅助判断，不得作为运动中实时航向主参考。

## 7100896修改

方向独立制动提前量按车体坐标生效：

| 方向 | 旧值 | 新值 | 数据依据 |
|---|---:|---:|---|
| 前 | 50mm | 50mm | 一格均值200.8mm，保持 |
| 右 | 50mm | 30mm | 一格均值178.0mm，约需晚制动20mm |
| 后 | 50mm | 47mm | 一格均值196.5mm，约需晚制动3mm |
| 左 | 50mm | 20mm | 基线及M1均明显少走，约需晚制动30mm |

斜线方向暂未启用，保守回退50mm。`POINT_STATUS`、`CAL_POINT`和`CAL_CFG`会打印实际`brake=...mm`，防止版本或方向混淆。

经过M1三次验证的300ms起步补偿已从模式3实验功能推广到所有`point_test`平移动作，包括正式模式0和点测模式2；旋转动作仍显式取消补偿。堵转保护继续保持300ms宽限、1000ms内不足5mm即STALL。PID、CPR4096、0.915、速度120和视觉门控均未修改。

## M2唯一验证

只跑一次QUICK12，它由12个单格动作组成，四方向各3次，每格均停车并等待延迟视觉。不要再跑GRID2_12或GRID3_12。

```text
重新上电静止3秒 -> 确认id=7100896、stage=DIRECTIONAL_BRAKE_M2、VISION CONNECTED且坐标合理 -> Z -> 2 -> 1 -> R -> 等待COMPLETE -> S
```

车放地图中部，四周至少留两格安全空间。运行中不推车；若出现STALL或PAUSED，不按C继续，保留现场并返回完整日志。正常完成后返回完整12段记录，并说明肉眼观察到的闭合偏移和是否有明显航向累积。

M2通过标准：12段无STALL、无人工推动；四方向单格编码器/稳定视觉误差约2至3cm内；闭合偏移较旧QUICK12明显降低且没有逐格航向发散。通过后停止制动重复测试，进入M3地图位置修正模型采集。

## M2实车结论：通过

用户使用7100896完成唯一一次QUICK12。全程12段均自主完成，最终`COMPLETE`、`valid=12`、`invalid=0`，无STALL、无PAUSED、无人工推动，全部记录`stable=6`、`bad=0`，串口`txdrop=0`。四方向独立制动提前量和全方向起步补偿均按配置生效。

四方向编码器单格结果如下：

| 方向 | 三次纵向结果/mm | 均值/mm | 最大横向绝对值/mm |
|---|---|---:|---:|
| 前 | 183.9 / 189.6 / 197.9 | 190.5 | 6.5 |
| 后 | 189.7 / 205.2 / 196.3 | 197.1 | 9.4 |
| 右 | 199.9 / 195.7 / 194.5 | 196.7 | 21.8 |
| 左 | 199.1 / 200.4 / 200.9 | 200.1 | 8.4 |

将每段纵向和横向编码器位移按实际方向还原到同一个二维坐标系后，12段闭环残差约为前后`-6.3mm`、左右`-1.5mm`，合成仅`6.5mm`。这比旧QUICK12约85mm的闭环残差大幅降低，证明方向独立制动提前量有效。前进均值仍短约9.5mm，但已落入基础完赛允许范围；禁止继续反复修改制动提前量或PID追求厘米级外观一致。

视觉绝对坐标首尾都回到约`(7.3,5.3)`，支持闭环成立。但视觉投影横向误差个别达到57.7mm，而同段编码器横向仅8.4mm；结合视觉角度多次在停车前后变化、IMU相对航向仅约数度，说明该大横向值主要被不稳定视觉角度污染，不能据此否定运动控制。M3必须使用视觉绝对位置或稳定位置差进行校正，视觉角度不得作为实时航向主参考，也不得直接拿来拟合车体横向误差。

M2正式通过，停止重复QUICK12制动验证，主线进入M3。当前代码、固件和识别号均未因本次分析改变：MCU仍为`7100896`，视觉仍为`oa_id=7100422`，AXF SHA256仍为`C755B5B2044ACCB68F5EAF07DBE0D24487A8868B733067DEDAC17C7F5980A980`。

## M3最小采集方案

本次地图中部QUICK12已作为中心区基线，不得在相同位置重复。下一步只在靠近左上区域、但离四周墙至少一格的位置再跑一次相同QUICK12，用绝对视觉坐标、编码器二维闭环和每方向残差判断位置相关性。若与中心区差异不超过约20mm且无视觉门控失败，则暂不实装查找表，直接推进M4；若出现稳定且可重复的区域偏差，再在右下区域补一轮并拟合最小的“方向 x 区域”修正表。

下一次唯一操作：

```text
重新上电并静止3秒 -> 将车放在空地图左上区域约(x=3,y=3)，四周至少空一格 -> 确认id=7100896、VISION CONNECTED且坐标合理 -> Z -> 2 -> 1 -> R -> 等待COMPLETE -> S
```

运行中不推车；若STALL或PAUSED，不按C继续。该轮用途是地图区域对照，不是重新验证M2。
