# 项目重启续接记录

生成时间：2026-06-14 21:27:14 +08:00

## 工作区

- 项目根目录：`C:\Users\L\Desktop\上赛道\协议层+算法层+中间层+控制层`
- 当前 IDE 打开的主要文件：`project\user\src\main.c`
- 当前工具工作目录：`project\mdk`
- 设计文档目录：`design`

## 用户已确认的约束

- 调参串口使用 `UART8`
- 算法部分箱子最多 3 个，不需要改成 4 个
- 规划中的 PC 端回归全图不用了
- 串口成功通信项目中的调试信息不要带入当前项目
- 后续修改应优先保持当前工程结构，不做无关重构

## 当前代码状态要点

- `project/code/ai_tuner.h`
  - 调参串口应保持为 `UART8`
  - `LPUART8_IRQHandler()` 当前调用 `ai_tuner_uart8_rx_handler()`
- `project/code/solver.h`
  - `MAX_BOXES` 当前为 3
- `project/code/vision_parser.h`
  - 视觉通信串口为 `UART4`
  - `VISION_PROTOCOL_MAX_BOXES` 当前为 4，这是协议解析容量，不等同于算法求解箱子数量
- `project/user/src/isr.c`
  - PIT 中断流程：`vision_parser_update()` -> `path_follower_update()` -> `motor_control_5ms()`
  - LPUART4 用于视觉协议接收
  - LPUART8 用于调参串口接收
- `project/user/src/main.c`
  - 主流程从视觉全图生成 `map_input_t`
  - 调用 `solve()` 生成任务动作
  - 调用 `path_follower_load()` 执行动作
  - `APP_ENABLE_VOFA_OUTPUT` 当前为 0，避免 VOFA 串口输出调试信息
- `project/code/avoidance_graph.c`
  - 当前仍为空实现
  - `micro_scheduler.c` 中会回退到 `car_bfs_path()`

## 最近完成的检查

已复核 `design\code_review_issues.txt` 中的 14 条问题，详细结论已保存到：

- `design\code_review_issues_verification.md`

复核结论概要：

1. `device_init_flag` 未加 `volatile` 是当前最重要风险
2. `vision_parser.c` 环形缓冲跨 ISR 访问问题真实，但严重度中等偏低
3. `back_forth_mode/back_forth_speed` 不是严重问题，报告中的 ISR 读取描述不准确
4. `BT_SPEED_SUB` 无下限保护真实，若蓝牙命令恢复使用则需要修
5. `avoidance_graph.c` 为空实现真实，但当前有 BFS 回退
6. 其余多为维护性、命名或诊断能力问题

## 建议下一步

若用户要求继续修改，建议按以下顺序执行：

1. 修复 `device_init_flag` 的 `volatile` 声明一致性
   - `project/code/blue.c`
   - `project/code/blue.h`
   - `project/user/inc/main.h`
2. 修复 `vision_parser.c` 环形缓冲索引可见性
   - 至少将 `rx_read_idx` 改为 `volatile`
   - 视需要再评估 PIT/LPUART4 中断优先级或临界区
3. 给 `blue.c` 的 `BT_SPEED_SUB` 增加下限保护
4. 优化 `main.c` 中 `mission_copy_vision_map()` 的全局关中断范围
5. 决定 `avoidance_graph.c` 是否实现，或明确标为 TODO 并保留 BFS 回退
6. 低优先级维护项：
   - `g_mission_error` 显示或上报
   - `VP_DMA_BUF_SIZE` 重命名
   - `isr.h` 去掉不必要 include
   - `motor_pwm_init()` 顺序与 `wheel_hw` 对齐

## 验证情况

- 本轮只做源码检查和文档保存，未修改 `.c/.h`
- 当前环境未完成 Keil/ARM 编译验证
- 如需真正确认编译，应在 Keil MDK 中重新构建 `project\mdk\rt1064.uvprojx`

## 注意事项

- 不要把旧串口成功通信项目里的大量调试输出迁回当前工程
- 若后续启用蓝牙命令，要重新确认 `bt_cmd_handler()` 的调用入口和中断上下文
- `user_config.h` 虽在 Keil 工程中列出，但源码当前未 include，文件头也说明它是旧版参考
- 当前重点不是把箱子数量扩成 4；算法层继续按最多 3 个箱子处理

## 2026-07-22 最新接力入口

- 当前MCU：`7100959 / RACE3_CONFIG_ALGO123`。
- 当前主线专项说明：`design/7100959_三图独立配置与三算法路由.md`。
- 集中配置入口：`project/code/app_config.h`和`project/code/app_config.c`。
- 默认正式构建：`APP_CONTROL_MODE_RACE + APP_UI_UART`。
- 图一算法1为`CLASSIC`，图二算法2为无炸弹识别`IMAGE_ONLY`，图三算法3为炸弹识别`BOMB_IMAGE`；三套生产算法最多处理3个箱子和3个目标，算法3另支持最多3个炸弹。
- 默认预设`SAFE_ALL`：三图都执行、速度120、SAFE策略、算法依次1/2/3。串口先用`6 -> C`查看，再`V`预检，最后`A -> V -> Y`启动。
- 离线双按键菜单状态机已实现但真实按键引脚未提供，当前`APP_BUTTON_HW_ENABLE=0`。
- 全局摄像头保持`oa_id=7100428`；第二摄像头保持`fp_id=7200102`且伪识别开启，两者本轮均未修改。

## FP2 7200103：双图片模型融合已接入

- 新模型位于`openart/模型`：两个图片模型共用`model_labels.txt`，一个数字模型使用`digit_labels.txt`。
- 二号摄像头已更新为`fp_id=7200103`：图片双模型0.80阈值融合、数字单模型0.80阈值、模型惰性加载和详细耗时诊断均已实现。
- 图片与数字原始0到9类别通过会话共享映射压缩为MCU需要的0到2，FP2协议和MCU代码不变。
- 当前伪识别仍开启。下一步先复位确认7200103自启动和链路，再关闭伪识别测真实模型耗时。
- 详细记录见`design/7100959_FP2双图片模型融合需求与实施规划.md`。

## FP2 7200104：ROI和IDE调试

- 二号摄像头当前为`fp_id=7200104`，ROI=`(55,40,245,180)`。
- `IDE_DEBUG_ENABLE=False`为比赛默认；改True后每500ms向OpenMV IDE打印真实识别结果。
- `IDE_DEBUG_MODE`支持IMAGE、DIGIT、BOTH；MCU请求优先，IDE调试不占用FP2结果通道。
- 详细记录见`design/7100959_FP2_ROI与IDE500ms调试.md`。

## FP2 7200105：模型存储路径诊断

- 7200104 实测出现`FP image labels unavailable`和`reason=NO_MODEL/status=2`；这表示 labels 在模型加载前就没有被运行时找到，不是融合冲突或阈值过高。
- H盘根目录已经确认存在三模型、两labels和两份脚本，故7200105扩展搜索到`/sd`、`/flash`、`/sdcard`、根目录和当前目录，并改用真实`open()`验证文件。
- IDE调试打开时新增`FP_FS`挂载点目录和`FP_FILE`逐路径诊断；比赛默认仍为`IDE_DEBUG_ENABLE=False`，ROI仍为`(55,40,245,180)`。
- 电脑源文件与`H:\main.py`、`H:\openart_roi_classifier.py`已经同步且哈希一致。
- 当前三端：MCU未改`7100959`；全局摄像头未改`7100428`；二号摄像头已改并同步`7200105`。
- 下一步只验证模型发现：复位二号摄像头，在IDE运行副本临时打开IMAGE调试，提交从`FP_FS_DIAG begin`到首个`FP_IDE_IMAGE`的完整日志。
- 详细记录见`design/7200105_FP2存储路径与NO_MODEL诊断.md`。
- 7200105实机已经确认`.`、`/sd`、`/flash`、`/sdcard`全部为`ENODEV`且`/`为空；根因是运行时整个文件系统未挂载，不是路径、阈值或融合逻辑。Windows当前仍占用`OPENMVSD (H:)`，下一步必须先停止脚本并安全弹出H盘，再保持USB连接复位和运行，不继续修改识别代码。
- 后续按弹出/复位流程复测通过：启动诊断瞬间仍可短暂`ENODEV`，但首次识别时`/sd`已挂载，`model_labels.txt`与两个图片模型均找到，两个模型均成功`load_to_fb`，输出`FP image models ready m1=1 m2=1 labels=10`。下一步只收集`FP_IMAGE_FUSE/FP_IDE_IMAGE`分类与耗时，不再调整存储路径。

## FP2 7200106：双模型改为串行驻留

- 7200105实机在两个约2.72 MB图片模型都`load_to_fb`成功后停止输出，首个`FP_IMAGE_FUSE/FP_IDE_IMAGE`没有出现；`IDE interrupt`仅是用户停止脚本。
- 根因优先判断为双大模型同时常驻帧缓冲后，首轮`tf.classify()`内存不足或阻塞，不再按路径问题处理。
- 电脑源文件已改为`fp_id=7200106`：加载模型1、推理、显式释放，再加载模型2、推理、释放，最后沿用0.80阈值融合。
- 新增`FP_IMAGE_STAGE`的`LOAD_BEGIN/INFER_BEGIN/RELEASED`日志，可在实机仍停住时直接定位阶段。
- 语法检查与4类融合分支测试通过；ROI仍为`(55,40,245,180)`，IDE图片调试仍为500ms，伪识别仍开启。
- `H:\main.py`与`H:\openart_roi_classifier.py`已同步7200106，且与电脑源文件SHA-256一致。
- 当前三端：MCU未改`7100959`；全局摄像头未改`7100428`；二号摄像头已修改并部署`7200106`。
- 详细记录见`design/7200106_FP2双模型串行推理与阶段诊断.md`。

## FP2 7200107：规避帧缓冲栈

- 7200106实机准确停在`image1 phase=INFER_BEGIN load=475ms`，因此阻塞点是首个`tf.classify()`，不是模型查找或双模型同时驻留。
- 已对比旧可运行`mobilenet.tflite`和三个新模型：大小、输入输出、算子集合与版本、张量类型和权重规模一致，模型结构不存在明显不兼容。
- 关键差异是旧脚本在抓图前加载模型，当前惰性加载在已有`snapshot()`后使用`load_to_fb=True`，会改变帧缓冲栈顺序并压缩临时张量空间。
- 电脑源已更新为`7200107`，只将模型加载切为`load_to_fb=False`，串行推理、0.80融合、ROI、IDE 500ms调试、伪识别和FP2协议均不变。
- 语法检查通过；当前`H:`未挂载，7200107尚未部署。
- 当前三端：MCU未改`7100959`；全局摄像头未改`7100428`；二号摄像头设备仍是`7200106`，电脑待部署源码为`7200107`。
- 详细记录见`design/7200107_FP2帧缓冲栈规避诊断.md`。

## FP2 7200109：硬崩溃启动循环恢复

- 7200107使用`load_to_fb=False`后仍停在`image1 phase=INFER_BEGIN load=484ms`，随后IDE失去整个USB设备且Windows TF卡消失，判定为`tf.classify()`触发主控硬崩溃/复位级故障，不是串口延迟。
- SD卡旧`main.py`默认打开500ms真实模型调试，每次重启都会再次推理并崩溃，形成反复断联的启动循环。
- 电脑端已拆分：安全持久化主程序`openart_roi_classifier.py`为`7200109`，关闭IDE真实推理且保持伪识别；独立诊断脚本`openart_roi_classifier_legacy_diag.py`为`7200108`，仅临时运行旧`mobilenet.tflite`做A/B测试。
- 两份脚本语法检查通过。当前`H:`仍未出现，7200109尚未写入设备。
- 恢复优先级高于继续测新模型：重新插入后立刻停止旧脚本，出现`H:`后把7200109写成`main.py`，确认30秒不掉线，再从IDE临时运行7200108。
- 当前三端：MCU未改`7100959`；全局摄像头未改`7100428`；二号摄像头待恢复到安全主程序`7200109`。
- 详细记录见`design/7200109_FP2崩溃启动循环恢复.md`。

- 首次向`H:`覆盖安全7200109时，驱动器在复制/哈希阶段再次掉线，是否已写入`main.py`尚未确认。
- 已准备99字节救援脚本`openart/fp2_rescue_main.py`，识别数`7200110`。若重插后仍是旧版本并掉线，下次挂载优先快速覆盖救援`main.py`，稳定后再部署完整7200109。
- 7200110救援脚本现已成功写入`H:\main.py`，长度99字节且SHA-256核对一致。下一步先安全弹出、重启并确认7200110持续在线，然后再写完整安全主程序7200109。
- 7200110实机确认持续在线后，完整安全主程序7200109已成功同步为`H:\main.py`和`H:\openart_roi_classifier.py`，哈希一致；旧模型A/B诊断脚本7200108另存为非自启动文件。下一步复位确认7200109与`ide_debug=0`持续在线，暂不运行真实模型。
- 7200108用普通内存惰性加载旧模型时出现`Failed to read requested bytes!`并在第二次重试停住，但这不等于旧模型失效，因为加载时序仍与历史成功版不同。
- 已从历史实机成功脚本生成真正的旧模型基线`openart/fp2_legacy_baseline_diag.py`，识别数7200111：启动时一次`load_to_fb=True`，之后循环复用模型；仅ROI更新为`(55,40,245,180)`。下一步从IDE临时运行7200111，观察是否恢复约3到4 FPS，绝不能保存成`main.py`。
- 7200111实机已恢复约3.62 FPS且持续稳定，确认硬件、TF卡、供电、固件和旧模型均正常。
- 已生成三个同生命周期的新模型独立基线：7200112图片模型1、7200113图片模型2、7200114数字模型，均启动前一次`load_to_fb=True`并循环复用。下一步依次临时运行，每个观察至少10帧，主程序7200109保持不动。
- 7200112、7200113、7200114实机全部稳定，帧率分别约3.59、3.60、3.61 FPS，确认三个新模型均正常。
- 电脑正式源码已重构为7200115：空闲主循环不抓图，每次严格按“加载模型->抓新帧->推理->删帧->删模型->GC”执行；图片双模型各完成一次生命周期后融合，数字模型独立执行。
- 独立融合诊断`fp2_ensemble_lifecycle_diag.py`为7200116，IDE图片调试开启。下一步只临时运行7200116，连续看到至少3轮双`RELEASED`和`FP_IMAGE_FUSE/FP_IDE_IMAGE`后，才把7200115部署为`main.py`。
- 当前三端：MCU未改7100959；全局摄像头未改7100428；二号摄像头设备仍为安全主程序7200109；电脑待验证正式源码7200115。
- 详细记录见`design/7200115_FP2多模型生命周期重构.md`。
- 7200116 实机停在第一模型首次 `INFER_BEGIN`，尚未触及模型2；三个新模型的单模型基线已确认正常，因此不再重复检查模型文件、TF卡、阈值或供电。
- 新增极简双模型切换诊断 `openart/fp2_compact_dual_model_diag.py`，识别数7200117。它显式调用 `tf.free_from_fb()` 并打印阶段内存；下一步只从IDE临时运行，观察至少三轮，严禁保存为`main.py`。
- 当前三端：MCU未改7100959；全局摄像头未改7100428；二号摄像头设备仍为安全主程序7200109；电脑候选正式源码7200115尚未部署。
- 详细记录见`design/7200117_FP2双模型极简切换诊断.md`。
- 7200117 已连续完成至少五轮双模型识别，第六轮亦继续正常；普通堆每轮稳定回到约4072160字节。此前第三轮日志截止不是卡死，无需采用识别后重启架构。
- 正式候选已更新为`openart/openart_roi_classifier.py`识别数7200118：模型加载前GC，推理后显式`tf.free_from_fb()`，清理置于`finally`，并移除启动文件系统遍历；FP2协议、ROI和融合规则不变。
- 完整协议诊断为`openart/fp2_full_protocol_diag.py`识别数7200119，仅IDE图片自动识别开启。下一步临时运行7200119至少三轮，通过后再部署7200118，严禁把7200119保存为`main.py`。
- 当前三端：MCU未改7100959；全局摄像头未改7100428；二号摄像头设备仍为安全主程序7200109；电脑正式候选7200118尚未部署。
- 详细记录见`design/7200118_FP2正式模型生命周期修复.md`。
- 7200119完整FP2外壳实机仍停在第一模型首次`INFER_BEGIN`，而7200117极简双模型可持续运行；因此故障与模型及释放逻辑无关，剩余候选仅为`cmm/UART12`初始化或完整协议常驻内存。
- 新增最终二分诊断`openart/fp2_compact_cmm_uart_diag.py`识别数7200120：在稳定7200117基础上只加入与正式代码相同的`cmm.add + UART12`。下一步仅临时运行到第一条`FP_DUAL_RESULT`；成功则拆分轻量推理工作模块，失败则重排/隔离CMM初始化。
- 当前三端仍未部署变化：MCU7100959；全局摄像头7100428；二号摄像头设备安全主程序7200109。电脑候选7200118暂不部署。
- 7200120在`cmm.add + UART12`下连续三轮双模型正常，故障已确定为7200119完整脚本常驻代码/状态过重，不是CMM或串口。
- 新增轻量完整FP2服务诊断`openart/fp2_compact_service.py`识别数7200121，约18.6KB，保留MCU实际依赖的会话、请求、结果、重发、探活、伪识别和真实映射，图片双模型及数字模型均使用已验证生命周期。
- 7200121当前每2秒自动做一次真实图片融合，仅允许IDE临时运行；下一步看到至少三条`FP_AUTO_IMAGE`后再生成并部署正式版。
- 当前三端：MCU7100959未改；全局摄像头7100428未改；二号摄像头设备仍是7200109。
- 详细记录见`design/7200121_FP2轻量比赛服务.md`。
- 7200121约18.6KB的单进程FP2服务在首次自动推理时仍使设备掉线，确认“协议状态机+TensorFlow同时驻留”不可作为比赛架构。
- 已改为进程隔离：`main.py/fp2_boot_dispatcher.py`启动分发；`fp2_listener.py`监听ID7200122；`fp2_image_worker.py`图片结果ID7200123；`fp2_digit_worker.py`数字结果ID7200124；公共协议/状态为`fp2_common.py`。真实请求通过20字节任务文件和受控复位切换，协议与模型不再同时驻留。
- 伪识别仍按约定默认True。H盘已写入五个文件并逐个SHA-256核对；待安全弹出和复位后生效。电脑`openart/openart_roi_classifier.py`也已改为分发器，旧7200118已备份。
- MCU已改为7100960，仅将FP2结果超时2000ms放宽到6000ms并更新构建标识；Keil全量构建0错误0警告。全局摄像头未改7100428。
- 下一步：安全弹出H盘并复位二号摄像头，确认`#FP2_LISTENER id=7200122 fake=1`持续在线；随后Keil下载7100960，先测伪FP2，再切换真实识别。
- 详细记录见`design/7200122_FP2进程隔离比赛架构.md`。

## 2026-07-22 FP2 7200126：伪链路通过，切换真实识别

- MCU `7100960` 与监听器 `7200122(fake=1)` 已实测完成会话初始化、箱子图片请求和目标点数字请求；`timeout/crc/fmt/drop/stale` 全为 0。
- 电脑源码 `openart/fp2_listener.py` 已切换为 `7200126(fake=0)`；真实请求将复位进入图片 worker `7200123` 或数字 worker `7200124`，完成后再回到监听器。
- `H:` 已挂载并完成部署：设备 `fp2_listener.py` 已切换为 `7200126(fake=0)`，电脑与设备 SHA-256 一致；旧设备监听器已备份。
- MCU 未修改，仍为 `7100960`；全局摄像头未修改，仍为 `7100428`；启动分发器仍为 `7200125`。
- 下一步先安全弹出 `H:` 并复位二号摄像头，确认 `#FP2_LISTENER id=7200126 fake=0`；再执行 `F -> I -> 等待 READY -> 1` 测图片，随后按 `4` 测数字。详见 `design/7200126_FP2真实识别切换.md`。

## 2026-07-23 FP2 7200127/7200128：复位任务交接修复

- 7200126 真实测试中图片和数字均超时，但监听器持续在线、协议错误为 0。挂载 H 盘后找到完整残留数字任务，且没有结果文件，确认 worker 没有启动。
- 根因是监听器写任务后仅等待 50 ms 即复位，旧分发器只读取一次；缓存提交或复位后存储就绪稍慢时会误回监听器。
- 新监听器 `7200127(fake=0)` 增加写入同步、三次读回校验和 300 ms 复位交接；失败会立即返回 `INTERNAL_ERROR`。
- 新分发器 `7200128` 对完整任务帧校验并以 50 ms 间隔最多重试 30 次；启动日志增加 `read=N`。
- 旧残留任务已备份删除，设备入口、监听器、公共协议和两个 worker 均已哈希核对一致。MCU 未改 `7100960`，全局摄像头未改 `7100428`。
- 下一步安全弹出 H 盘并复位，再按 `F -> I -> READY -> 1` 测图片，监听器恢复后按 `4` 测数字。详见 `design/7200127_7200128_FP2任务交接修复.md`。

## 2026-07-23 FP2 7200129/7200130：极简模型 Worker

- 任务交接修复后图片仍超时；失败现场为 job 已被删除而 last 不存在，确认 worker 已进入但在结果落盘前退出。数字命令当时在图片 `REQUEST_WAIT` 中发送，被 MCU 正确拒绝，尚未实际测试数字 worker。
- 根因方向收敛到 worker 常驻规模：旧形态同时驻留分发器、worker 和 4.2KB `fp2_common`，与此前完整协议外壳导致首轮推理退出一致。
- 图片 worker 改为单文件 `7200129`（约6.1KB），数字 worker 改为单文件 `7200130`（约5.8KB），均不再导入 `fp2_common`；分发器导入前释放读取/校验函数。
- 新增 `fp2_trace.bin` 阶段标记，若再失败可直接从残留字节定位阶段。设备已同步并哈希核对。
- MCU 未改 `7100960`，全局摄像头未改 `7100428`；监听器仍为 `7200127`，分发器仍为 `7200128`。
- 下一步：安全弹出 H、复位，执行 `F -> I -> READY -> 1`；必须等待图片结果和 READY 后再按 `4`。详见 `design/7200129_7200130_FP2极简模型Worker.md`。
- 7200129 第二轮现场中 job 已被消费，但 last 和最早 trace 均不存在，同时 Windows 已重新挂载 H。判断为复位后 USB 大容量存储被电脑重新占用，worker 失去 `/sd`，而非模型推理本身卡死。下一轮必须断开二号摄像头 USB 数据线、使用赛车独立供电，仅从 MCU 无线串口测试；本轮不再改代码。

## 2026-07-23 FP2 7200131/7200132/7200133：三帧均值与可靠结果重发

- 最新现场 `fp2_last.bin` 已包含数字 worker `7200130` 生成的合法结果，证明分发、模型推理和结果落盘全部成功；MCU 超时收敛为 worker 复位后单次结果帧未可靠送达。
- 监听器更新为 `7200131`：只对当前 session 的合法结果非阻塞重发 4 次，首发延时 80 ms、间隔 120 ms；新会话清除旧结果，避免串包。
- 图片 worker 更新为 `7200132`：两个模型各采 3 帧，对完整类别概率求均值后再执行双模型融合；数字 worker 更新为 `7200133`：单模型 3 帧类别概率均值。阈值均保持 0.80。
- MCU 更新为 `7100961 / FP2_MULTI3_REPLAY`，结果等待放宽到 10000 ms；控制、规划和比赛运动行为未改。全局摄像头未改，仍为 `7100428`；分发器仍为 `7200128`。
- 三个设备脚本已同步 `H:` 并完成 SHA-256 核对；Python 语法检查通过；Keil 完整构建 `0 Error(s), 0 Warning(s)`。
- 下一步：安全弹出 H、复位二号摄像头、Keil 下载 7100961，然后严格执行 `F -> I -> READY -> 1 -> 等待结果及READY -> 4`。详见 `design/7200131_7200133_FP2三帧均值与结果重发.md`。

## 2026-07-23 MCU 7100962：FP2 同请求重试

- 7100961 实测图片和数字仍超时；等待期间接收计数每 10 秒只增加约 5 个探活包，`proto/match/stale/crc/fmt` 均不增加，说明不是结果匹配失败，而是没有结果协议帧送达。
- MCU `REQUEST_WAIT` 已增加同 session/request/type/slot 的 1 秒周期重发，最多 8 次，总超时仍为 10 秒。它可触发监听器已有的持久结果重复请求回传，不会重复执行已完成的识别。
- `FP2_STATUS` 新增 `retry=`。Keil 完整构建 `0 Error(s), 0 Warning(s)`。
- 本轮只修改 MCU 为 `7100962 / FP2_REQUEST_RETRY`；全局摄像头未改 `7100428`；二号摄像头未改，仍为 `7200131/7200132/7200133`，分发器 `7200128`。
- 下一步只下载 MCU7100962，再执行 `F -> I -> READY -> 1 -> 等结果及READY -> 4`。详见 `design/7100962_FP2同请求重试与持久结果取回.md`。

## 2026-07-23 FP2 7200134/7200135：极简三帧均值 Worker

- 7100962 重试实测仍超时；挂载现场为最新数字 job 存在、trace=`0x24`、last 不存在，证明监听器收到了请求且数字 worker 已进入模型加载/推理段，退出点在新增三帧 worker 内。
- 图片和数字 worker 在不取消三帧类别概率均值的前提下压缩：每帧只取唯一 ROI 分类输出并立即累加，数字不再读取内容固定为0到9的 labels，删除不可见长打印。
- 图片 worker 更新为 `7200134`（6003B），数字 worker 更新为 `7200135`（5539B）；均回到旧单帧成功版规模附近。监听器仍为 `7200131`，分发器仍为 `7200128`。
- 两文件已同步 H 且哈希一致，遗留任务/结果/trace 已清理。MCU未改 `7100962`，全局摄像头未改 `7100428`。
- 下一步安全弹出H并复位，按 `F -> S` 确认监听器在线，再执行 `R -> I -> READY -> 1 -> 等READY -> 4`。详见 `design/7200134_7200135_FP2极简三帧均值Worker.md`。

## 2026-07-23 FP2 7200136/7200137/7200138：软复位交接修复
- 新现场中合法数字任务 `fp2_job.bin` 仍存在，而 `fp2_trace.bin`、`fp2_last.bin` 均不存在，证明监听器已经落盘，但 `machine.reset()` 后分发器/Worker 从未进入；上一轮“仍在模型阶段退出”的判断被新证据修正。
- 监听器更新为 `7200136`，任务交接改为 `machine.soft_reset()` 优先、`machine.reset()` 次级、1秒硬件看门狗最终兜底；交接前图片/数字分别写 `0xA1/0xA2` 阶段码。
- 图片 Worker 更新为 `7200137`，数字 Worker 更新为 `7200138`，结束返回也统一采用软复位优先；三帧概率均值与模型逻辑不变。分发器仍为 `7200128`。
- 已同步 H 并核对哈希，旧 job/state/trace/last 已清理。下一步安全弹出 H、复位二号摄像头，执行 `F -> S -> R -> I -> READY -> 1 -> READY -> 4`。
- 专项记录：`design/7200136_7200138_FP2软复位交接修复.md`。

## 2026-07-23 FP2 7200139：放弃复位分发，改为常驻直连
- 7200136 实测收到图片请求后立即离线，之后 Worker 和监听器都未重新上线；复位分发架构不再继续修补。
- 二号摄像头改为单进程常驻直连服务 `7200139`：UART12收请求后直接加载模型、三帧均值、回包并继续监听，无任务文件、无复位、无分发器/Worker切换。
- 图片双模型、数字单模型、0.80阈值、共享紧凑ID映射、重复请求回包和4次结果发送均保留。运行入口约8.6KB，基于已连续实测成功的极简CMM/UART双模型生命周期。
- 已同步为H盘main.py与openart_roi_classifier.py并核对哈希。下一步安全弹出H、复位，执行 `F -> S -> R -> I -> READY -> 1 -> READY -> 4`；在线ID应始终为7200139。
- 专项记录：`design/7200139_FP2常驻直连服务.md`。

## 2026-07-23 FP2 7200140：会话初始化兼容修复
- 7200139持续探活正常，但收到SESSION_INIT后未回ACK并静默，故障发生在模型调用之前。
- 固定20/10字节协议帧改为预分配后原位赋值，并在接收包处理外围增加异常隔离，避免目标MicroPython容器差异终止整个服务。
- 已部署7200140并核对哈希。下一步只执行 `F -> R -> I`，看到READY后再测模型；MCU7100962和全局摄像头7100428未改。

## 2026-07-23 FP2 7200140：实机会话握手通过
- 实机执行 `F -> S -> R -> I` 后，MCU `7100962` 已从 `SESSION_WAIT` 正常进入 `READY`，状态为 `session=1 ready=1 count=3/3 status=OK`。
- `proto=1` 且 `timeout/crc/fmt/drop=0`，UART1 B12/B13 到摄像头 UART12、SESSION_INIT 和 ACK 全部确认正常。
- 不再重复测试初始化。下一步按 `1 -> 等待READY -> 4 -> 等待READY -> S` 验证图片与数字结果链；每个请求间必须等待识别完成。
- 当前识别号：MCU `7100962`；全局摄像头 `7100428`；二号摄像头 `7200140`。本次仅更新接力文档，三端代码均未修改。

## 2026-07-23 FP2 7200141：请求推理调用栈收缩
- 7200140首个图片请求后停止探活，MCU重试8次仍无结果；SESSION握手已通过，所以故障定位为真实图片推理入口。
- 二号摄像头源代码更新为7200141：UART处理仅登记任务，主循环直接调用模型；图片暂时只用模型1但保留3帧均值和0.80阈值，数字链与协议不变。
- 语法检查及主机协议/模型生命周期模拟通过，SHA-256为`8B0AB23ECEF6A9691576B3CC7C1EA90F417C7EB57037D374791D977D23968AFA`。
- 7200141已部署到H盘`main.py`和`openart_roi_classifier.py`，电脑源文件及两个设备入口均为9056字节，SHA-256一致：`8B0AB23ECEF6A9691576B3CC7C1EA90F417C7EB57037D374791D977D23968AFA`。设备旧入口备份位于`design/_codex_backups/20260723_DEVICE_BEFORE_FP2_7200141`。
- 下一步安全弹出H盘并复位，严格执行`F -> S -> R -> I -> READY -> 1`，只验证图片结果，不要在超时或离线后继续发数字请求。
- 本轮MCU仍为7100962、全局摄像头仍为7100428，二者代码均未修改。

## 2026-07-23 FP2 7200141：图片结果链实机通过
- 7200141已实机完成会话初始化与箱子槽位0图片识别，结果为`id=0 confidence=100% status=OK`。
- 推理期间摄像头未离线，结果后恢复READY；MCU计数`proto=2 match=1 timeout/crc/fmt/drop=0`。
- 图片链主线已经关闭，不再回退7200140或复测图片入口。下一步同一会话只按`4`验证数字模型，期望`proto=3 match=2`并保持在线。
- 当前三端：MCU7100962；全局摄像头7100428；二号摄像头7200141。本次只更新文档，代码未修改。

## 2026-07-23 FP2 7200141：数字结果链实机通过
- 数字槽位0请求返回`confidence=60% status=LOW_CONFIDENCE id=255`，MCU匹配计数升至2并恢复READY，摄像头保持在线，timeout/crc/fmt/drop均为0。
- 至此SESSION、图片模型、数字模型和双向结果协议全部打通。60%仅是当前画面的模型置信度低于0.80阈值，下一步改为对准清晰真实数字后重复测试准确率。
- `stale=4`主要由结果重发造成，暂不作为故障处理。本次只更新文档，三端代码未修改；识别号保持7100962/7100428/7200141。

## 2026-07-23 FP2 7200141：数字重新对准后100%通过
- 同一数字目标重新对准后的第二次请求返回`id=1 confidence=100% status=OK`，摄像头在线并恢复READY，累计match=3且无timeout/crc/fmt/drop。
- 下一步保持同一画面只再按两次`4`；若ID仍为1且置信度均超过80%，停止独立相机测试，回归N-2实车观察与三图比赛主线。
- 本次只更新文档，三端代码未修改；识别号仍为MCU7100962、全局7100428、FP2 7200141。

## 2026-07-23 FP2 7200141：配对语义待用户确认
- 新会话箱子图片槽位0为`id=0/95%/OK`；数字槽位0最终为`id=1/98%/OK`（此前另有61%、77%低置信度）。
- 双模型与协议均正常，但两个紧凑ID不同。若现场二者确为对应一对，则当前语义映射不可用于N-2；若不是对应一对，则结果合理。
- 当前不改代码。下一步先取得本次图片人物名称和实际数字真值，再决定添加固定图片到数字映射，还是只修正画面/模型。

## 2026-07-23 FP2 7200141：喜羊羊与数字3确认不匹配
- 用户确认现场是一对“喜羊羊 + 数字3”。两份labels中它们的原始类别都应为3，因此共享紧凑ID也必须一致。
- 现场图片`id=0/95%/OK`而数字`id=1/98%/OK`，确认当前结果语义错误；N-2实车暂不放行，但SESSION、模型执行、回包和MCU匹配链均已打通。
- 下一步先运行独立数字诊断脚本读取数字3的原始`index`；期望为3。图片侧此前独立测试已多次得到`pleasant_sheep/index=3`，当前优先排查数字模型/ROI/标签顺序。
- 本次只更新记忆文档，代码未修改；识别号保持MCU7100962、全局摄像头7100428、二号摄像头7200141。

## 2026-07-23 FP2 7200141：配对错误已确认具有重复性
- 同一“喜羊羊 + 数字3”画面中，数字连续8次稳定返回紧凑`id=1`（93%至99%），图片连续7次稳定返回紧凑`id=0`（99%至100%）；前两次数字低置信度属于对位过渡。
- 全程无timeout/crc/fmt/drop且每次回到READY，故不是通信或请求匹配故障，而是稳定的分类语义不一致。
- 当前日志没有raw model index，不能继续靠重复按1/4定位。下一步必须用独立数字诊断脚本验证数字3的原始`index`，或扩展保留字段回传raw-id。
- 本次只更新记忆文档；MCU7100962、全局摄像头7100428、二号摄像头7200141均未修改。

## 2026-07-23 数字3基线确认模型误判
- 独立数字基线7200114对准真实数字3约140帧，几乎全部为raw index8、少量index7、从未出现index3，且index8最高约98%。
- 因此正式FP2返回不同紧凑ID的根因在数字分类输入/模型，不在共享ID映射、MCU或串口；降低0.80阈值无益。
- 下一步仅测试真实数字8：若输出3则排查3/8标签互换，若仍输出8则确认3/8视觉混淆并转向ROI/训练集修正。
- 本次仅更新文档，运行代码与识别号不变：MCU7100962、全局摄像头7100428、FP2正式服务7200141；7200114只是IDE临时诊断脚本。

## 2026-07-23 MCU 7100963：C12/C13/C14 离线屏幕菜单
- 当前暂停数字识别精度支线，回归完整三图比赛主线，先验收无无线串口条件下的 IPS200 离线控制。
- `APP_UI_MODE` 已切为 `APP_UI_SCREEN`，正式比赛分支不初始化无线串口和 PC 菜单。C12/C13/C14 均为内部上拉、低电平按下，与屏幕、电机、编码器和两路摄像头串口无冲突。
- C12短按上移、长按取消；C13短按下移、长按急停；C14短按修改、长按两次依次准备和启动。屏幕可编辑预设及三轮各自的RUN、策略、速度和算法。
- 修改前备份为`design/_codex_backups/20260723_3KEY_OFFLINE_SCREEN_BEFORE_7100963`；Keil完整构建0错误0警告，AXF SHA-256为`9E8A0D3B6DAB91524E816120AAE82284DB06F9F5B1A220DD0AFA893776BE07A4`。
- 当前三端：MCU已改`7100963 / OFFLINE_3KEY_SCREEN`；全局摄像头未改`7100428`；二号摄像头未改`7200141`。
- 下一步不接无线串口，先只验收屏幕显示、C12/C13选项移动、C14逐项修改及“一次长按ARM、C12长按取消”；纯菜单通过后才在赛道验收第二次C14长按启动和C13长按急停。
- 专项记录：`design/7100963_三按键离线屏幕菜单.md`。

## 2026-07-23 MCU 7100964：离线屏幕消除全屏闪烁
- 7100963每250ms先全屏清黑再重画，形成明显黑帧。7100964改为仅初始化时清屏一次，之后用补满30字符的整行原位覆盖；无事件刷新降为500ms，按键事件仍立即刷新。
- 按键、比赛配置和运动逻辑未改。Keil完整构建0错误0警告，AXF SHA-256=`A40A96689416F7C2E7AE480E2F498CAB62F1DEA04C99B44F00C16D115D0068A9`。
- 当前三端：MCU已改`7100964 / OFFLINE_SCREEN_NO_FLICKER`；全局摄像头未改7100428；二号摄像头未改7200141。
- 下一步下载7100964，先静置观察10秒，再短按C12/C13/C14；预期不再周期黑屏、选项立即更新且无残影。
- 专项记录：`design/7100964_离线屏幕消闪烁.md`。

## 2026-07-23 MCU 7100965：SKIP 前进后原航向退回
- 旧SKIP在出基地后仍请求地图、等待视觉稳定，再按AUTO返航策略使用视觉/BFS回基地，与现场要求不符。
- 新SKIP固定执行编码器+IMU前进450mm；点到点内部原航向停车保持500ms后，立即以车体180度后退相同450mm。全程不请求地图、不等待视觉、不进入BFS。
- 新增状态`SKIP_RETURN`，屏幕显示`SKIP_BACK`；每轮之间等待由1秒改为2秒。RUN轮次逻辑完全未改。
- 三轮全SKIP时预检只要求IMU；任意轮为RUN时仍要求IMU和全局视觉。Keil完整构建0错误0警告，AXF SHA-256=`00923171F82209CC523DB83312DFD064FCFDD7C52BFE3531EA51602C1B18F8AD`。
- 当前三端：MCU已改7100965；全局摄像头未改7100428；二号摄像头未改7200141。修改前备份为`design/_codex_backups/20260723_7100965_BEFORE_SKIP_ENCODER_RETURN`。
- 下一步把三轮都设为SKIP进行一次完整三轮测试，观察每轮前45cm、停0.5秒、后45cm、轮间2秒及最终COMPLETE。
- 专项记录：`design/7100965_SKIP编码器原路返回.md`。

## 2026-07-23 MCU 7100966：运行模式与界面独立开关
- `main.c`用户配置区恢复为直接数值入口：`CONTROL_DEBUG_ENABLE`选择0正式/1六模式/2点到点/3赛道采集；`RACE_UI_MODE`独立选择0 UART或1离线屏幕，只影响正式模式。
- 六模式参数也恢复到同一区域直接修改。`app_config.h`中重复的`APP_CONTROL_MODE/APP_UI_MODE/APP_VERIFY_*`已删除，避免两个配置源不一致。
- 模式1仍固定屏幕，模式2/3仍固定UART，不受正式比赛界面开关影响。UART和SCREEN两档均完成Keil构建，全部0错误0警告；最终恢复为正式比赛+离线屏幕。
- 当前MCU`7100966 / USER_MODE_UI_SWITCH`，最终AXF SHA-256=`43D61DF49FD649FD79C9B2014211EB80A687A88EDFE559880F6267E1CF35C028`。摄像头未改，保持7100428/7200141。
- 下一步下载7100966确认离线菜单；以后恢复无线串口只改`RACE_UI_MODE`为`RACE_UI_UART`并重新编译。
- 专项记录：`design/7100966_独立运行与界面开关.md`。
# 2026-07-23 MCU7100967 UART菜单 + 实体启停

- 最终配置改为`CONTROL_DEBUG_ENABLE=0U`、`RACE_UI_MODE=RACE_UI_UART`。
- UART8继续负责全部三图配置；C14长按900ms负责ARM/START，C13长按负责紧急停车。
- UART模式短按C12/C13/C14全部忽略；开机按住的键必须先释放，避免误触。
- 串口主比赛菜单、诊断菜单及按键模拟中的软件START均已禁止；推荐流程为`6 -> V -> A -> 长按C14`。
- 离线屏幕菜单保留，且单独回归编译通过。
- 版本：MCU7100967；全局摄像头7100428未改；二号摄像头7200141未改。
- 详细现场流程见`design/7100967_UART菜单实体启停与现场流程.md`。
- 赛场色域硬时限流程见`design/赛场5分钟色域快速采集计划.md`。

## 2026-07-23 全局摄像头7100429：比赛屏幕新色域

- 已用`地图采样/无格子`六张新样本和VR同名地图真值重标定全局摄像头；比赛屏幕特征为边缘黑域、中央略过曝。
- 五类单色图内部格心离线回放均140/140；map666为138/140，两个差异均位于车体色块遮挡处。
- 空地/目标使用共同蓝紫色域加`a+b=-20`分界，避免暗空地误墙和紫目标被判蓝空地；未加入位置相关补偿。
- 当前版本：MCU7100967未改；全局摄像头源码7100429待下载确认；第二摄像头7200141未改。
- 下一步只做一次实机FULL_MAP数量核对，通过后冻结。回退文件为`openart/新视觉main_backup_7100428_before_competition_screen_lab_20260723.py`。

## 2026-07-23 MCU7100968：算法1扩容到5箱

- 主线新增能力：算法1 `CLASSIC`支持最多5箱/5目标；算法2和算法3继续显式限制3箱，FP2和炸弹核心未扩容。
- 经典调度器已由固定三进度改为5项进度数组，匈牙利矩阵、Action容量和状态池同步扩容。
- 全局摄像头源码升级为7100430，使用`FULL_MAP_V2=0x02/64字节`发送5箱/5目标；MCU仍兼容旧V1 60字节全图帧。
- 原回归18/18通过；新增4箱、5箱简单/混合/复杂四图全部通过独立逐步物理回放；算法2/3的3箱上限测试通过。
- Keil全量构建0错误0警告；AXF SHA-256=`2D865DBBBFD30C6C6BF2C8C2284DD4063FF32BA72A9369B7804F7960E65EAA34`。
- 当前三端：MCU源码及AXF已改7100968；全局摄像头源码已改7100430但未确认下载；第二摄像头未改7200141。
- 修改前备份：`project/mdk/backup_20260723_134655_before_classic5`；专项记录：`design/7100968_算法1五箱扩容.md`。
- 下一步必须先部署7100430和7100968，再用一帧FULL_MAP确认5箱计数，随后只执行`2 -> S`干跑解算。
