# 7100958 规划世界连续拒绝后自动复核返航

## 日志结论

- 本次实际运行固件为 `7100957 / MANUAL_WITHDRAW_RETURN`；开头出现的 `7100956` 是无线串口缓存残片。
- 本轮成功离开基地、读取首张 `3箱/3目标/3炸弹` 地图，并完成了多段任务与运行时重规划。
- 后期 FULL_MAP 持续变为 `3箱/2目标/0炸弹`。该世界计数不满足求解器输入约束，规划器返回 `WORLD_REJECTED`。
- 旧状态机对规划失败没有次数上限，每次都回到 `WAIT_NEW_MAP`。日志中地图版本从 5 持续增加到 17，元素计数始终不恢复，所以车辆锁定且无限请求地图。
- 日志中没有收到 `D` 或 `R`，因此 7100957 新增的人工 `D -> R` 主动结束本轮功能没有被触发。

## 7100958 修改

- 连续规划拒绝允许刷新地图 3 次；前两次保持原有恢复逻辑。
- 第 3 次仍不可规划时，停止任务运动，将本轮标记为未完成，自动进入三帧完赛复核。
- 复核地图未清空时沿用现有 BFS 返航；地图清空时沿用直接返航。
- 本轮首张有效地图的初始箱子、目标和炸弹数量只记录一次，不再被后续异常 FULL_MAP 覆盖。
- 状态输出新增 `reject=x/3` 与 `initial_valid`，便于区分瞬态刷新和即将自动返航。
- 人工 `D -> R` 保底入口继续保留。
- 未修改电机/编码器映射、PID、制动、速度、IMU航向、任务动作、全局摄像头或第二摄像头。

## 验证

- Keil ARMCLANG 6.16 构建：`0 Error(s), 0 Warning(s)`。
- Program Size：`Code=258688, RO-data=35824, RW-data=684, ZI-data=632744`。
- AXF SHA256：`75213DAB8594EE37DD4977B6BA766E14917B8BBC2B375F35F96BF4C09C194EDF`。
- 修改前备份：`design/_codex_backups/20260722_035315_7100957_BEFORE_BOUNDED_WORLD_REJECT`。

## 下一步

优先直接验证主动非清图返航，不必再等待同一异常自然出现：下载 7100958 并完全断电上电，执行 `6 -> 1 -> C -> 2 -> 7 -> P -> Q -> S -> A -> V -> Y`。车辆稳定执行且地图仍有箱子时发送 `D -> R`。预期依次出现 `ROUND WITHDRAW accepted -> FINISH_MULTI_MAP -> RETURN_PREP -> RETURN_PATH -> ROUND_DONE -> MATCH_COMPLETE`。

若自然再次出现世界计数不一致，应看到 `reject=1/3`、`2/3`、`3/3`，随后自动进入 `FINISH_MULTI_MAP`，不得继续无限刷新地图。
