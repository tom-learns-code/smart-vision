# 7100918 旋转制动提前角7度

## 7100917 实测分析

入口门控修复已经通过实车验证：两次 `R` 均正常执行，随后 `Q` 成功完成两段往返，`valid=2 invalid=0 result=OK`。因此上一版的“Q/R无反应”已经解决。

90度旋转的IMU结果如下：

- 单次CW：最终 `+94.0deg`，误差 `+4.0deg`；主动停止点 `90.3deg`，停车后余转 `3.7deg`。
- 单次CCW：最终 `-94.6deg`，误差 `-4.6deg`；主动停止后余转约 `3.7deg`。
- Q转出CCW：最终 `-93.7deg`，误差 `-3.7deg`；主动停止后余转约 `6.3deg`。
- Q返回CW：回到原始航向后越过约 `3.9deg`；主动停止后余转约 `5.0deg`。

与7100916的双向约`97.6deg`相比，7100917已把典型误差从约`7.6deg`降到约`4deg`，说明末段40速度和提前制动方向正确。四次记录的动态余转约`3.7..6.3deg`，因此下一步只把提前角由3度增加到7度。

视觉角度仅作参考：四次视觉变化约`96.2..103.0deg`，其中一次稳定簇跨度`6.3deg`，与IMU不一致，继续禁止其参与实时旋转停止和启动门控。编码器反推的有效轮心半径为`88.7..90.6mm`，均值约`89.6mm`；该结果记录供后续编码器旋转里程标定使用，本轮不修改当前`100mm`配置，因为当前旋转停止依据是IMU。

## 7100918 修改

- MCU识别号：`7100918 / ROT_BRAKE_LEAD7`。
- 旋转目标前20度的末段速度继续保持`40 counts/5ms`。
- 主动旋转提前制动角由`3deg`改为`7deg`。
- 原目标航向保持、IMU控制、方向、电机/编码器映射、PID、平移制动、正式比赛链路和两台摄像头均未修改。

四档`CONTROL_DEBUG_ENABLE=0/1/2/3`均经ARMCLANG 6.16构建，全部`0 Error(s), 0 Warning(s)`。最终`project/mdk/Objects/rt1064.axf`保留模式3，SHA256=`128CA65EC93DC4AC084219DB7CADCD79F9A27C37F92388388646B4D19C942EBE`。

完成态检查点：`_codex_backups/checkpoint_7100918_rotation_brake7_built_20260720_045000`。

## 下一轮验证

完整重新上电后：

1. `7 -> 2 -> R`，等待第一条完整`ROT_REC`。
2. `D -> R`，等待第二条完整`ROT_REC`。
3. `Q`，等待两条`OUT_BACK ROT_REC`和`state=COMPLETE`。

目标是四条IMU最终误差绝对值尽量落入`0..2deg`，且往返后净航向误差不超过`2deg`。同时人工观察车体是否基本回到格线方向。未达到前不运行AUTO16，也不改编码器中心半径。
